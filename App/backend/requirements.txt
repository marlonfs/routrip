fastapi>=0.111
uvicorn>=0.30
pywebview>=5.1
httpx>=0.27
pillow>=10.3
# OCR: detecção + reconhecimento PP-OCR em ONNX. Roda in-process, sem binário externo.
# Os modelos ficam em vendor/ocr e são baixados por scripts/get_ocr_models.ps1.
rapidocr>=3.9,<4
onnxruntime>=1.20,<2
# O rapidocr depende de `opencv-python`; manter também o -headless instalado deixaria
# dois pacotes servindo o mesmo `cv2`. O 5.x reorganizou o pacote e o hook do PyInstaller
# ainda não acompanha: o exe empacotado morre com falha nativa (sem traceback) na
# primeira chamada ao cv2.
opencv-python>=4.9,<5
numpy>=2,<3
pydantic>=2.7
python-multipart>=0.0.9
openpyxl>=3.1
pypdfium2>=4.30
pyinstaller>=6.6
